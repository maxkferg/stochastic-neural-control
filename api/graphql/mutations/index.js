const UserMutations = require('./user');
const MeshMutations = require('./mesh');
const RobotMutations = require('./robot');

module.exports = {
    UserMutations,
    MeshMutations,
    RobotMutations,
};